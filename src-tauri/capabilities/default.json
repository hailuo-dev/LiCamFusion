{
  "$schema": "../gen/schemas/capabilities.json",
  "identifier": "default",
  "description": "Default capability for LiCam application",
  "windows": ["main"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-emit",
    "dialog:default",
    "fs:default"
  ]
}
